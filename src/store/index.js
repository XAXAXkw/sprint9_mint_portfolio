import { createStore } from 'vuex'
import json from '../store/DB.json'

export default createStore({
  state: {

    DB:json
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
